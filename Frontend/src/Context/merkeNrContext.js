//Alador

/**
 * Arrayen av sauer fra hjemmesiden eksporteres på denne måten til andre sider.
 * den brukes deretter i auto complete feltene til far og mor i updateSheep og Registrering av sau
 */
import React from "react";

const MerkeNrContext = React.createContext([]);

export default MerkeNrContext;
